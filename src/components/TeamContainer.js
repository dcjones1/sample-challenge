import React from 'react'
import Grid from '@material-ui/core/Grid'
import CharacterCard from './CharacterCard'

const TeamContainer = () => {
  return (
    <div>
      {/* What needs to happen here? Hinthint: look inside CardContainer!! it's the exact same. */}
      <h3>My adventure team should display here!</h3>
    </div>
  )
}

export default TeamContainer
